---
---
<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width,initial-scale=1" />
		<title>rii-init.net</title>
		<link rel="icon" href="/favicon.svg" />
	</head>
	<body>
		<div id="ripple-bg"></div>
		<main>
			<slot />
		</main>
		<style>
			:root {
				--glass-bg: rgba(255,255,255,0.25);
				--glass-border: rgba(255,255,255,0.18);
				--text-color: #111827;
			}
			@media (prefers-color-scheme: dark) {
				:root {
					--glass-bg: rgba(30,30,40,0.35);
					--glass-border: rgba(255,255,255,0.12);
					--text-color: #f3f4f6;
				}
			}
			html, body {
				min-height: 100%;
				height: 100%;
				margin: 0;
				padding: 0;
				font-family: Inter, Roboto, Arial, sans-serif;
				background: linear-gradient(135deg, #c9e7ff 0%, #f5cee7 100%);
				color: var(--text-color);
				overflow-x: hidden;
			}
			#ripple-bg {
				position: fixed;
				inset: 0;
				z-index: 0;
				pointer-events: none;
				background: repeating-radial-gradient(circle at 50% 60%, #b3e5fc 0 10%, #f5cee7 15% 20%, transparent 25% 100%);
				opacity: 0.6;
				animation: ripple 12s linear infinite;
				filter: blur(40px);
			}
			@keyframes ripple {
				0% { background-size: 100% 100%; }
				100% { background-size: 120% 120%; }
			}
			main {
				position: relative;
				z-index: 1;
				min-height: 100vh;
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				gap: 2rem;
				padding: 2rem 0;
			}
		</style>
	</body>
</html>
